<h1>つぶやき一覧</h1>

<table>
  <tr>
    <th>本文</th>
  </tr>
  <%= @blogs.each do |blog| %>
    <tr>
    <td><%= blog.content %></td>
    <td><%= link_to '編集', blog_path(blog.id) %></td>
    </tr>
   <% end %>
</table>
<br>
<%= link_to '新しいつぶやきを投稿する', new_blog_path %>
